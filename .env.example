# Variables d'environnement pour sssdwnld_2

# -- Backend --
NODE_ENV=production
PORT=3000

# URL du frontend pour la configuration CORS en production
# Mettre plusieurs URLs séparées par une virgule si nécessaire
CORS_ORIGIN=https://sssdwnld.com

# Limites de requêtes API (par IP)
RATE_LIMIT_WINDOW_MIN=15
RATE_LIMIT_MAX_REQUESTS=100

# -- Monitoring (Optionnel) --
# Si vous utilisez un service comme Sentry ou Datadog
# SENTRY_DSN=

# -- Sécurité --
# Une chaîne de caractères aléatoire pour sécuriser certaines opérations futures
APP_SECRET=remplacer_par_une_longue_chaine_aleatoire